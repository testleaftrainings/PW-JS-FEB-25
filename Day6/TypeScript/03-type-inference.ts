
/* Type inference

1. Implicit type inference 
When the TS compiler is able to infer the datatype on its own from the value that the user assigns

2. Explicit type inference

*/

//implicit type inference

let num =40;
let message ="Hello"

//Explicit type inference

let statusCode :number = 200;
console.log(statusCode);

let itIsaGreatDay = true;
